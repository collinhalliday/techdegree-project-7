extends layout

block main
  section.desk
    h1= `${err.message} - Status: ${err.statusCode}`
    img.desk-items(src='/images/desk.svg')
    a.button-primary-small(href='/') Go Home
    .paper
      pre.stack-trace= err.stack
    .tear
  script(src="/js/error.js")
